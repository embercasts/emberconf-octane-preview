<form {{action this.submitChanges on="submit"}}>
  <InputField @type="text" @label="First Name" @value={{this.first}} @onchange={{action (mut this.first)}} />

  <InputField @type="text" @label="Last Name" @value={{this.first}} @onchange={{action (mut this.last)}} />

  <div class="form-footer">
    <button type="submit" class="btn-submit">save</button>
  </div>
</form>
