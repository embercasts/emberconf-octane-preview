<select ...attributes onchange={{action @onchange value="target.value"}}>
  <option>{{@label}}</option>
  {{#each @options as |item|}}
    <option selected={{eq @value item}} value={{item.id}}>{{yield item}}</option>
  {{/each}}
</select>
