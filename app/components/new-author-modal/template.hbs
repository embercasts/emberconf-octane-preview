<button type="button" class="btn-trigger" {{action (mut this.showModal) true on="click"}}>Add a New Author</button>

{{#if this.showModal}}
  <div class="modal-if">
    <div class="modal">
      <form class="modal-card" onsubmit={{action this.save}}>
        <h3 class="modal-heading">Add New Author For Book</h3>

        <div class="modal-content">
          {{input-field type="text" label="First Name" value=this.author.first onchange=(action (mut this.author.first))}}

          {{input-field type="text" label="Last Name" value=this.author.last onchange=(action (mut this.author.last))}}
        </div>

        <div class="modal-footer">
          <button type="button" class="btn-clear" {{action (mut this.showModal) false on="click"}}>Cancel</button>
          <button type="submit" class="btn-submit">Submit</button>
        </div>
      </form>
    </div>
  </div>
{{/if}}
